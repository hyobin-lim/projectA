{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "prettier --write"]
  }
}